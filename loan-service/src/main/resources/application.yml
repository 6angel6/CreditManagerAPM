spring:
  application:
    name: loan-service
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/loanservice
    password: 666
server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

